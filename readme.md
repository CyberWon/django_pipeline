# 说明

实验性项目。功能并不正常！仅仅是能运行！

# 后端运行

使用django开发，运行前修改settings.py里面的mysql，redis，执行migrate同步数据结构就能运行


# 前端运行
```shell
cd frontend/desktop
npm install
npm run dev
```

# 验证

上面步骤没问题的话，访问http://localhost:9000 就可以了
